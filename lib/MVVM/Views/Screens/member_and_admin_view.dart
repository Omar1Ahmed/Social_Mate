import 'package:flutter/material.dart';

class MemberAndAdminView extends StatelessWidget {
  const MemberAndAdminView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(" MemberAndAdminView"),
      ),
    );
  }
}
